package java_gui;

import javax.swing.*;
import java.awt.*;

public class Main {
    public static void main(String[] args) {
/*
        JFrame frame = new JFrame(); // create a frame
        frame.setTitle("JFrame Title Here"); // sets title of frame
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // exit out of application
        frame.setResizable(false); //prevent frame from being resized
        frame.setSize(420,420); // sets the x and y dimension of the frame
        frame.setVisible(true); // make frame visible

        //not supported in mac!
        ImageIcon image = new ImageIcon("fractal_logo.png"); // create an ImageIcon
        frame.setIconImage(image.getImage()); // change icon of frame



        frame.getContentPane().setBackground(new Color(123,50,150)); // change color of background, can also use hexadecimal instead*/

        //MyFrame myFrame = new MyFrame(); //OR just new MyFrame
    }
}